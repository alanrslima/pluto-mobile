import {StyleSheet} from 'react-native';

export const monthPickerStyles = () => {
  return StyleSheet.create({
    shadow: {
      position: 'absolute',
      left: 0,
      top: 0,
      bottom: 0,
      right: 0,
    },
  });
};
