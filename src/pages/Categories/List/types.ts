import {NativeStackScreenProps} from '@react-navigation/native-stack';
import {RootStackParamList} from '../../../types/RootStack';

export type CategoriesListPageProps = NativeStackScreenProps<
  RootStackParamList,
  'CategoriesListPage'
>;
