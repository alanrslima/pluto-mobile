import {NativeStackScreenProps} from '@react-navigation/native-stack';
import {RootStackParamList} from '../../../routes/types';

export type TransactionsDetailPageProps = NativeStackScreenProps<
  RootStackParamList,
  'TransactionsListPage'
>;
